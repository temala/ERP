import { ClientInvoiceStatusColorPipe } from './client-invoice-status-color.pipe';

describe('ClientInvoiceStatusColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientInvoiceStatusColorPipe();
    expect(pipe).toBeTruthy();
  });
});
